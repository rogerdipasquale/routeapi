## Testing
This directory contains json files to be used in Go tests to run local tests.

`scripts` directory contains files to run tests on a local kubernetes cluster, in order to test interaction with deployed objects.
